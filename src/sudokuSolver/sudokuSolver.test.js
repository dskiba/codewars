import { sudokuSolver } from './sudokuSolver';

describe('sudokuSolver', () => {
  it('sudokuSolver does this thing...', () => {
    const input = null
    const res = null
    expect(sudokuSolver(input)).toEqual(res)
  });
});
