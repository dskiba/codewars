import { parseInt } from './parseInt';

describe('parseInt', () => {
  it('parseInt does this thing...', () => {
    const input = null
    const res = null
    expect(parseInt(input)).toEqual(res)
  });
});
