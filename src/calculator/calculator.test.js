import { calculator } from './calculator';

describe('calculator', () => {
  it('calculator does this thing...', () => {
    const input = null
    const res = null
    expect(calculator(input)).toEqual(res)
  });
});
