import { intervalIntersection } from './intervalIntersection';

describe('intervalIntersection', () => {
  it('intervalIntersection does this thing...', () => {
    const input = null
    const res = null
    expect(intervalIntersection(input)).toEqual(res)
  });
});
